# The codegen gate's ceilings: per watched function, a substring of its demangled name, the
# blessed instruction count, and the blessed escape count. Blessed generously today because both
# counts drift across compiler versions and platforms (the pinned Linux clang outlines more
# calls and emits fewer instructions than Apple clang for the same source); the campaign
# tightens these into budgets once the box gives normal a number (docs/PRACTICE.md). Raising a
# ceiling is a deliberate act that belongs in the diff that needed it, with the ritual's report
# beside it.
onCommand 380 16
onSubmission 280 8
